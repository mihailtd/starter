{
  "name": "@starter/db",
  "private": true,
  "version": "0.0.0",
  "description": "The Prisma DB Migrations for the Starter",
  "scripts": {
    "gm": "yarn graphile-migrate",
    "migrate": "yarn gm migrate",
    "watch": "yarn gm watch",
    "commit": "yarn gm commit",
    "uncommit": "yarn gm uncommit",
    "reset": "yarn gm reset"
  },
  "dependencies": {
    "graphile-migrate": "^1.2.0"
  },
  "devDependencies": {
    "@types/pg": "^8.6.4",
    "pg": "^8.7.1"
  }
}
