bases:
  - ../../bases/client
  - ../../bases/pg
  - ../../bases/db
  - ../../bases/server
  - ../../bases/gql
  # - ../../bases/keycloak
  - ../../bases/hanko
  - bases/mailhog
  - bases/client
  - bases/server

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: dev-env-configmap
    envs:
      - .env
  - name: hanko-config
    files:
      - hanko-config.yaml
  - name: db-initialize-configmap
    files:
      - init.sql

namespace: starter
