apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: hanko-ingress
  namespace: starter
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`hanko.local.starter.com`)
      priority: 10
      services:
        - name: hanko-service
          port: 80
  tls:
    certResolver: default
    options:
      name: hanko-service
