apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: hanko-ingress-web
  namespace: starter
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`hanko.local.starter.com`)
      priority: 10
      services:
        - name: hanko-service
          port: 80
      middlewares:
        - name: https-redirect
